
    // Include to use log frontend
    // Initialization:
    //      initp::log::setup(); - use stdout
    //      initp::log::setup(app_name, config); - load proto config
    // Use:
    //      initp::log::error(func, e) << <message>;
    //      initp::log::info(func) << <message>;

#include <initp/log/frontend/logger.hpp>
